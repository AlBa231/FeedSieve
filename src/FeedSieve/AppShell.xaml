<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="FeedSieve.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:sf="clr-namespace:Syncfusion.Maui.Toolkit.SegmentedControl;assembly=Syncfusion.Maui.Toolkit"
    xmlns:pages="clr-namespace:FeedSieve.Pages"
    FlyoutBehavior="Flyout"
    Title="FeedSieve">

    <FlyoutItem Title="Feeds" Icon="{StaticResource IconFeedList}" Route="feeds">
        <Tab Title="Today" Icon="tab_today.png">
            <ShellContent x:Name="TodayContent" Route="today" />
        </Tab>
        <Tab Title="New" Icon="tab_new.png">
            <ShellContent x:Name="NewContent" Route="new" />
        </Tab>
        <Tab Title="All" Icon="tab_all.png">
            <ShellContent x:Name="AllContent" Route="all" />
        </Tab>
        <Tab Title="Saved" Icon="tab_saved.png">
            <ShellContent x:Name="SavedContent" Route="saved" />
        </Tab>
    </FlyoutItem>

    <ShellContent
        Title="Projects"
        Icon="{StaticResource IconProjects}"
        ContentTemplate="{DataTemplate pages:ProjectListPage}"
        Route="projects" />

    <ShellContent
        Title="Manage Meta"
        Icon="{StaticResource IconMeta}"
        ContentTemplate="{DataTemplate pages:ManageMetaPage}"
        Route="manage" />

    <Shell.FlyoutFooter>
        <Grid Padding="15">
            <sf:SfSegmentedControl x:Name="ThemeSegmentedControl" 
                VerticalOptions="Center" HorizontalOptions="Center" SelectionChanged="SfSegmentedControl_SelectionChanged"
                SegmentWidth="40" SegmentHeight="40">
                <sf:SfSegmentedControl.ItemsSource>
                    <x:Array Type="{x:Type sf:SfSegmentItem}">
                        <sf:SfSegmentItem ImageSource="{StaticResource IconLight}" SemanticProperties.Description="Light mode"/>
                        <sf:SfSegmentItem ImageSource="{StaticResource IconDark}" SemanticProperties.Description="Dark mode"/>
                    </x:Array>
                </sf:SfSegmentedControl.ItemsSource>
            </sf:SfSegmentedControl>
        </Grid>
    </Shell.FlyoutFooter>

</Shell>
